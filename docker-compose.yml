services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: tegen
      POSTGRES_PASSWORD: tegen
      POSTGRES_DB: tegen
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
